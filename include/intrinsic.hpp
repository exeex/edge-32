#ifndef EDGE32_INTRINSIC_COMPAT_HPP
#define EDGE32_INTRINSIC_COMPAT_HPP

// Canonical edge-32 include. The explicit filename remains available for code
// that needs to coexist with the legacy RV64 Edge intrinsic header.
#include "edge32_intrinsic.hpp"

#endif
