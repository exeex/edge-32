# Edge32 physical targets

This tree owns physical targets for the Edge32 product adapter and its composed
scalar blocks. Shared RV leaf implementations remain sourced from `edge-rv`,
while Edge32 owns the composition profile and timing target.
